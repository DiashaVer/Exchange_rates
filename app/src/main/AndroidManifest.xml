<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- часть 3.5 (обработка разрешений) и часть 3.1 (доступ в интернет) для HTTP-запросов,
    без него Retrofit не сможет выполнять сетевые запросы-->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- часть 5.3 (push-уведомления) и 5.5 (корректная обработка разрешений)
    разрешение требуется на POST_NOTIFICATIONS aandroid 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExchangeRates">

        <!-- часть 2 (навигация между фрагментами - MainActivity )
        и 4 (MVVM) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ExchangeRates">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>